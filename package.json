{
  "name": "white-label-task",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter player-shell dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm --filter player-shell lint"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
